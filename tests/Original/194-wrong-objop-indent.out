<?php
function index() {
	$obj->attribute
	    ->with(['a' => 'b'])
	    ->into($var);

	$obj->attribute
	    ->with()
	    ->into($var);
}

$obj
	->with(['a' => 'b'])
	->into($var);

$obj->attribute
    ->with(['a' => 'b'])
    ->into($var);
