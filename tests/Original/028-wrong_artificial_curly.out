<?php
function func() {
	if (!$t) {
		return (false);
	}
	if (is_null($r)) {
		return ($t);
	}
	while ($r = mysql_fetch_assoc($r)) {
		// comment 1
		if ($torf) {
			$tt = 0;
			if ($fort) {
				$tt = 1;
			}
			$obj = new ClassObject();
			$obj->set_sku(htmlentities($arr['s']))
			    ->set_quantity(1)
			    // Comment 2
			    ->set_amount($tt) // Comment 3
			    // Comment 4
			    ->set_product_name(htmlentities($arr['p'] . '_' . $arr['q']));

			$obj2->add_item($obj);
		}
	}
}